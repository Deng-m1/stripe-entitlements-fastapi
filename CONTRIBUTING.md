# Contributing

Read `AGENTS.md` and the invariant documents before changing billing behavior. Open an
issue for material policy changes. Pull requests must describe the invariant, event
permutations, database locking order, migration impact, and exact verification commands.

All contributions must be original or license-compatible with Apache-2.0. Never include
real Stripe keys, signing secrets, customer payloads, or production database extracts.
